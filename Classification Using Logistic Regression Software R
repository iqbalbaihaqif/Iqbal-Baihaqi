#Importyourfile
> library(readxl)
> yourfile <- read_excel("C:/Users/Iqbal Baihaqi F/Downloads/yourfile.xlsx")
> View(yourfile)
> library(caret)
> library(caTools)
> mydata<-data.frame(yourfile)
> mydata
#splitdataset
> set.seed(123)
> split=sample.split(mydata,SplitRatio = 0.8)
> training=subset(mydata,split==TRUE)
> testing=subset(mydata,split==FALSE)
#reglogalgorithm
> modelreglog<-glm(y~Age+Job+marital+education+balance+housing+contact+duration,data=training,family=binomial(link="logit"))
> modelreglog
